apiVersion: v1
kind: Service
metadata:
  name: core-gateway
  namespace: core-services
  labels:
    app.kubernetes.io/name: core-gateway
    app.kubernetes.io/part-of: appkiosk-core
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: core-gateway
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
